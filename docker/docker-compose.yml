services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: prefect
      POSTGRES_PASSWORD: prefect
      POSTGRES_DB: prefect
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prefect -d prefect"]
      interval: 5s
      timeout: 5s
      retries: 5

  prefect-server:
    image: prefecthq/prefect:3-python3.13
    restart: unless-stopped
    command: >
      bash -c "pip install prefect-aws && prefect server start --host 0.0.0.0"
    ports:
      - "4200:4200"
    environment:
      PREFECT_API_DATABASE_CONNECTION_URL: postgresql+asyncpg://prefect:prefect@postgres:5432/prefect
      PREFECT_SERVER_API_HOST: 0.0.0.0
      PREFECT_UI_API_URL: /api
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build:
      # Context is the en_banc repo root; juriscraper and jkent are sibling
      # repos pulled in as named build contexts (BuildKit / Compose v2).
      context: ..
      dockerfile: docker/Dockerfile.worker
      additional_contexts:
        juriscraper: ../../juriscraper
        jkent: ../../jkent
    image: en-banc-worker
    restart: unless-stopped
    environment:
      PREFECT_API_URL: http://prefect-server:4200/api
      SCRAPER_RUNS_DIR: /app/runs
      # Max JKent runs this worker executes at once; start another worker for
      # more capacity. Override on the host: WORKER_CONCURRENCY=8 docker compose up
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-4}
    volumes:
      - worker_runs:/app/runs
    depends_on:
      - prefect-server

volumes:
  postgres_data:
  worker_runs:
