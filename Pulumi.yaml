name: en-banc-foundation
description: "Foundation: Docker network, databases, Prefect server, LocalStack"
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
